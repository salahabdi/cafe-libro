import React, { Component } from 'react';
import './styles/buttons.css'
import {Link, useHistory} from 'react-router-dom';

const UpdateButton = () => {
    return (

        <button type="button" className="btn btn-lg">Update</button>
    );
}

export default UpdateButton;